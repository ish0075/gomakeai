import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1 style={{ color: 'white', textAlign: 'center', marginTop: '100px' }}>
      Meta GTP Chat UI Coming Soon...
    </h1>
  </React.StrictMode>
);
